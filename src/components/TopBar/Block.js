/**
 * Module contains content block in the right side of the container
 * @module components/TopBar/Block
 * @author Igor Ivanov
 */
import styled from 'styled-components';

const Block = styled.div`
  align-items: center;
  background-color: transparent;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
`;

export default Block;
